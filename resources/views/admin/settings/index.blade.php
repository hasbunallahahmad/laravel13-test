<x-layouts::admin :title="__('Settings')">
    <div class="space-y-6">
        <div>
            <flux:heading size="xl">
                {{ __('Settings') }}
            </flux:heading>

            <flux:text class="mt-2">
                {{ __('Manage website settings.') }}
            </flux:text>
        </div>

        {{-- Konten settings --}}
    </div>
</x-layouts::admin>
